import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.quoteswift.app',
  appName: 'QuoteSwift',
  webDir: 'dist/apps/mobile/browser',
};

export default config;
